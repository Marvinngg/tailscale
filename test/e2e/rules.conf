# 测试用广告拦截规则（QuantumultX 格式）
# 模拟墨鱼规则的结构

# 开屏广告 → 直接拦截
^https?://ad-server:8888/ad/splash url reject-dict

# 混合内容 API → JS 脚本精确去除广告字段
^https?://ad-server:8888/api/feed url script-response-body remove_ad.js

# MITM 声明
hostname = ad-server
